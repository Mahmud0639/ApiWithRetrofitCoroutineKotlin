<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ApiWithRetrofitCRUD" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ApiWithRetrofitCRUD" parent="Base.Theme.ApiWithRetrofitCRUD" />

    <style name="CustomTabLayout" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabIndicatorColor">@color/your_indicator_color</item>
        <item name="tabTextColor">@color/your_unselected_text_color</item>
        <item name="tabSelectedTextColor">@color/your_selected_text_color</item>
        <item name="android:background">@color/your_tab_background_color</item>
    </style>

    <style name="MyCustomFABRound" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">@dimen/_30mdp</item>
    </style>
</resources>